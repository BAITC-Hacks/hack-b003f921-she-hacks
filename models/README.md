# Model artifacts

Store trained models, fitted preprocessors, and evaluation reports here.
Generated files are ignored by Git. This folder starts empty; train a model
before implementing inference against a saved artifact.
