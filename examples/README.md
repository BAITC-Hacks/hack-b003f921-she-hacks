# Synthetic integration fixtures

`synthetic_weather_input.csv` contains four invented weather rows for testing
the batch interface. They are NOT real weather forecasts, observations, or
training data. February dates are arbitrary interface examples and do not
implement a February execution loop. `synthetic_weather_output.csv` is generated
from these invented inputs using the saved models; its predictions are not
accuracy metrics.
